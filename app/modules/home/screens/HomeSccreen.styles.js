import {StyleSheet, Dimensions} from 'react-native';

const styles = StyleSheet.create({
  buttonwrapper: {
    alignItems: 'center',
        paddingVertical: 5,
        height: 50,
        margin: 10,
  },
  
})

export default styles;